---
/**
 * Column wrapper for subpage “hover to expand” copy blocks.
 * Pairs with `SubpageHoverSectionItem.astro` — shared styles in `global.css` (`.subpage-hover-sections` …).
 */
---
<div class="subpage-hover-sections">
  <slot />
</div>
