# Source assets (not deployed)

Files here are **not** copied to the site. Astro only deploys what lives under `public/`.

Put uncompressed or oversized sources here (e.g. original video masters). Compress or transcode into `public/` before shipping (respect Cloudflare Workers’ per-file size limits for bundled assets).
